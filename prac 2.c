#include <stdio.h>

int main() {
    double length = 70;
    double breadth = 90;

    double area = length * breadth;
    double perimeter = 2 * (length + breadth);

    printf("Area: %.2f\n", area);
    printf("Perimeter: %.2f\n", perimeter);

printf("25ce097\n Manthan");

    return 0;
}
